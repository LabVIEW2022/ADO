import pifacedigitalio as p
p.init()

class Cool:
    def __init__(self, unit):
        self.unit = unit
    def OPN (self, OPN)
        self.Sensor = p.digital_read(7)
        # self.CLS = p.digital_write(2, 0)
        # self.Sensor = p.digital_read(7)

a = test('1')
print (a.Sensor)